# Phase 2: 有名フラクタル・カタログ（8 種）

## 目的

アプリの表現（**折れ線の基図形** ＋ **相似変換のリスト** ＋ **深さ**）で再現できる範囲で、一般に名前が通じるフラクタルを揃える。各エントリは `base_shape`・`replicas`・推奨 `depth`（および任意で `show_all_generations`）で完全に記述できること。

## 選定の前提

- **相似IFSのみ**: `Replica` は一様スケール・回転・平行移移動の合成。バーンスレイ・シェルピンスキー台形のように**異方スケールやシアー**が本質のものは、類似IFSで**雰囲似**にするか、カタログから外すかを表で明示する。
- **識別しやすさ**: メニューラベルは英語の通称（必要なら括弧で別名）で統一。実装後、Result を見て「その名前だと分かる」ことを受け入れの目安とする。

## プリセット一覧（実装 8 種）

| # | メニューラベル（案） | 種・ノート | replicas の狙い |
|---|----------------------|------------|-----------------|
| 1 | Sierpiński triangle (gasket) | 正三角形の辺 | 3 頂点方向へ 1/2 縮小・配置の古典 IFS |
| 2 | Koch curve (classic) | 単一線分 | 4 相似（端部 2 ＋中央バンプ） |
| 3 | Vicsek fractal (cross) | 正方形の枠 | 中心＋四隅へ 5 相似 |
| 4 | Heighway dragon | 水平線分 | 2 相似のドラゴン曲線 IFS |
| 5 | Lévy C curve | 水平線分 | 2 相似（いずれも同じ回転角の別並進） |
| 6 | Sierpiński carpet | 正方形の枠 | 中央を除く 8 相似 |
| 7 | Pythagoras tree | 水平線分（斜辺） | 頂点 (0,h) の直角三角形の 2 相似。**既定 depth 12・Show generations ON** |
| 8 | Sierpiński hexagon | 正六角形の辺 | 6 頂点方向へ 1/3 縮小 |

**除外（実装しない）**: Cantor set（線分上の単調IFS）、Binary tree（汎用二分木の類）——メニューの面白み優先で省略。

## 幾何・パラメータの方針

- **スケール**: 既存の `REPLICA_SCALE_MIN` / `MAX` 内に収める。はみ出しが大きいプリセットは翻訳を縮小する。
- **深さ**: プリセットごとに推奨値。ピタゴラスの木は見た目のため深め（既定 12）と全世代表示を既定 ON にする。
- **検証**: 各プリセットについて、深さ 1〜最大付近で**指数爆発がUI上許容**かを確認（既存の depth 上限と整合）。

## 計画概要

| ID | 内容 |
|----|------|
| T1 | 列挙型（または定数テーブル）で全体プリセット ID を定義し、`FractalState` を構築する関数をモジュールに集約。 |
| T2 | メニュー表示名と内部 ID の対応。国際化は必須としない。 |
| T3 | 上表を満たすようパラメータを詰め、外れ値は表を更新して確定する。 |

## 受け入れ条件

- [ ] `overall_plan.md` の Phase 2 チェックリストを満たす。
- [ ] 上表の **8 行すべて**がメニューから個別に選べる。
- [ ] 相似 IFS で厳密再現が難しい項目はコード側コメントまたは本 md で追える。
